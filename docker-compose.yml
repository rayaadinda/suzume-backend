services:
  api:
    build: .
    container_name: task-backend
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      # These will be overridden by your .env file on the server
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS}
      - JWT_SECRET=${JWT_SECRET}
    restart: always
